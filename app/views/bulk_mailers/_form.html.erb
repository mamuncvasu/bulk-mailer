<%= form_with(model: bulk_mailer) do |form| %>
  <% if bulk_mailer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bulk_mailer.errors.count, "error") %> prohibited this bulk_mailer from being saved:</h2>

      <ul>
        <% bulk_mailer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :to, style: "display: block" %>
    <%= form.text_field :to %>
  </div>

  <div>
    <%= form.label :cc, style: "display: block" %>
    <%= form.text_field :cc %>
  </div>

  <div>
    <%= form.label :subject, style: "display: block" %>
    <%= form.text_field :subject %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :mail_attach, style: "display: block" %>
    <%= form.file_field :mail_attach, multiple: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
